import { Grid } from "@mui/material";
import RecipeThumbnail from "../components/RecipeThumbnail";
import { RecipeType } from "../types";

export const Search = () => {
    return (
        <div>
            <h1>Search</h1>
            {/* make a grid of RecipeThumbnail cards */}

        </div>
    );
};

export default Search;
